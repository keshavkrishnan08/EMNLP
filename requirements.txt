# Core pipeline (analysis + config). Lets the package import and the tests run.
pyyaml>=6.0
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Training / evaluation stack (GPU). Pinned loosely; the sweep targets Kaggle T4.
torch>=2.1
transformers>=4.40
tokenizers>=0.15
safetensors>=0.4
accelerate>=0.28
datasets>=2.18
huggingface_hub>=0.23   # download.py fetches BabyLM files directly via the Hub
stanza>=1.7

# Optional: LLM-as-judge for filter QA. Only needed with `--judge llm`.
# anthropic>=0.25

# Dev
pytest>=7.4
ruff>=0.4
